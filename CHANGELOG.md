# 변경 이력

날짜는 작업 기록 기준이며 운영 배포일과 다릅니다. 실제 배포 여부는 [진행표](docs/07-progress.md)에서 확인합니다.

## 다음 배포 — 로컬 검수 중 (2026-09-14)

### 추가·변경

- 승인 없는 가입, 학생의 내 예약 목록·상세·시작 전 취소, 학번·이름·비밀번호 수정.
- 학생 비밀번호를 숫자 6자리로 단순화하고, 이관 계정의 비밀번호 변경 안내에 `나중에 하기`를 추가.
- 관리자 역할·예약 소유권 검사, CSRF, POST 로그아웃, DB 기반 시도 제한, 비밀번호 변경 시 다른 세션 무효화.
- 학생 비밀번호 6자리 숫자와 해시 저장, 원문 PIN 컬럼 제거, 이관 학생의 비밀번호 변경 유도, 공개 초기 관리자 비밀번호 차단.
- 기존 `logo.ai`에서 제공된 웹용 `logo.png`를 공통 헤더에 사용하고, 후원 문구 푸터를 예약 안내로 정리.
- 좌석 번호와 테두리의 기본 대비를 높이고, 최대 이용시간을 초과하는 즉시 시간 선택 아래에 경고하도록 개선.
- 좌석 화면을 항공사형 선택 흐름으로 개편하고, 좌석 클릭 시 구역별 모니터·GPU·DXRacer 의자 정보를 확인한 뒤 선택하도록 변경.
- PC 1–10을 `Evnia Performance Zone`, PC 11–27을 `Evnia Gaming Zone`으로 색상과 텍스트 범례로 구분하고 기존 별표를 제거.
- 실제 배치에 맞게 왼쪽 벽면 좌석은 왼쪽을 향하고, 가운데 두 열은 서로 마주보도록 모니터 방향을 표시.
- 320px 모바일 화면에서도 27개 좌석이 좌석실 테두리 안에 들어오도록 카드 여백과 좌석도 크기를 조정.
- 관리자 내비게이션에 `공지·설정`을 추가하고 예약 화면의 상단 공지와 이용 안내를 각각 수정할 수 있게 변경.
- 예약을 30분 단위의 `시작 시간 + 이용 시간` 방식으로 바꾸고, 당일 지난 시간과 운영 종료 초과를 선택 즉시 안내.
- 학생 예약 취소를 시작 전 또는 예약 생성 후 5분 이내로 제한하고, 이후 상세 화면에서 취소 버튼을 숨김.
- 학생 로그인·가입 직후 좌석 예약 화면을 먼저 열고, 내 예약은 상단 메뉴에서 확인하도록 흐름을 정리.
- 이관 계정의 `나중에 하기`는 현재 로그인 세션에서만 숨기고 다음 로그인 때 비밀번호 변경 안내를 다시 표시하도록 규칙을 명확히 함.
- 차단을 매주 반복되는 수업, 단발성 전체 대관, 여러 날짜·여러 좌석 일괄 점검의 세 가지 운영 흐름으로 분리.
- Alembic `0003`에서 기존 시간 단위 예약·차단·점유 슬롯을 분 단위로 보존 변환.
- Alembic `0004`에서 예약 접속 IP 저장 필드를 추가하고 관리자 예약 목록에서만 표시.
- 변경되지 않는 회원 ID로 예약·제한 연결, 사용했던 학번의 소유권 보존.
- 한국 시간·공통 운영 규칙, 예약 FK·좌석 시간/회원 일일 고유 제약, SQLite 쓰기 트랜잭션.
- 학생증 대조 체크인·이용 종료, 사유 있는 체크인 정정·노쇼 기록, 시간/좌석 차단, 운영 기록.
- 앱 팩토리·환경변수 설정, 빈 DB용 Alembic `0001`, 읽기 전용 기존 DB에서 별도 새 DB로 이관하는 명령.
- 회귀 테스트와 인수인계·배포·Git 규칙·기술 선택 문서 정리.

### 배포 전 확인

현재 운영 기준은 PythonAnywhere의 `f05a5c2`입니다. 이 항목의 로고·좌석 대비·30분 예약·차단 방식·공지 설정 개선은 로컬 검수 후 한 번에 커밋·push·배포합니다. 배포 전 DB 백업과 `flask db upgrade`가 필요합니다.

## 초기 진단 문서 — `9b0367e`

초기 코드 `e0681a0`의 진단과 기능·디자인·개발·배포·개관 가이드, Git 작업 규칙을 보존했습니다. 이 진단 커밋 자체는 기존 문제의 수정이나 배포 완료를 의미하지 않습니다.

## 커밋 기록

- `8f8bfb3` — 예약·인증·스키마·이관·테스트·CI 구현
- `bc37d27` — 후임자용 시작 안내 추가
